name: Zizmor

# Static analysis of this repository's own GitHub Actions workflows.
#
# The trap this file is shaped around: `zizmor-action` defaults to
# `advanced-security: true`, which runs zizmor with `--format=sarif`, and zizmor
# deliberately exits 0 under SARIF — the consumer is expected to decide what
# blocks. A single job would therefore upload findings to the Security tab and
# go green forever. Only `--format=github` (selected by `annotations: true`)
# exits non-zero on a finding.
#
# Hence two jobs, because one job cannot both upload SARIF and fail:
#
#   report  every audit zizmor has, at the widest persona, uploaded to code
#           scanning. Never fails. This is the full ruleset, including the
#           pedantic and auditor-only audits, so nothing is invisible.
#
#   gate    the subset worth blocking a merge on, annotated on the offending
#           line. Fails the build.
#
# The gate blocks on two floors, and they answer different questions. Confidence
# is whether a finding *is* real; severity is how bad it would be if it were.
# `min-confidence: medium` keeps speculative findings from spending a red check.
# `min-severity: low` keeps the two purely advisory audits from doing the same:
# `self-repository` prefers GitHub's newer `$/...` syntax over the `./...` this
# repository uses, and `superfluous-actions` would rather softprops/action-gh-
# release were a `gh release upload` call. Both are style, both are things to
# decide on deliberately rather than under a failing check.
#
# The report job below runs at the auditor persona with no floor at all, so
# everything the gate lets through is still recorded — strictness here costs no
# visibility, which is the whole reason the two jobs are separate.
#
# Which audits run, and which publishers we are willing to run at all, live in
# .github/zizmor.yml — zizmor discovers that file itself, so neither job passes
# a `config:` input.

on:
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:

permissions: {}

# One run per pull request, and per branch for a push.
#
# Cancelled for a pull request, because a superseded commit's findings are an
# answer about code nobody is going to merge. Not cancelled for a push to
# master: the report job uploads to code scanning, and cancelling it would leave
# the Security tab describing an older commit with nothing saying so.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  report:
    name: Report to code scanning
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read # To check out the repository below.
      security-events: write # For upload-sarif, which zizmor-action wraps.
    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
      - name: Run zizmor
        uses: zizmorcore/zizmor-action@3dc1ecc9bcb9e94e9b2c709687979e1298497054 # v0.6.2
        with:
          # The widest persona: every pedantic and auditor-only audit on top of
          # the regular set. No severity or confidence floor — this job is the
          # record, not the gate, so nothing is filtered out of it.
          persona: auditor
          # `all` ignores .gitignore and picks up composite actions, the
          # Dependabot config and the pre-commit config alongside the workflows.
          collect: all
          online-audits: true
          advanced-security: true

  gate:
    name: Gate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read # To check out the repository below.
    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
      - name: Run zizmor
        uses: zizmorcore/zizmor-action@3dc1ecc9bcb9e94e9b2c709687979e1298497054 # v0.6.2
        with:
          # Mutually exclusive with advanced-security, which is why this cannot
          # be the same job as the upload. `annotations` selects
          # `--format=github`, and with it a non-zero exit on findings.
          advanced-security: false
          annotations: true
          collect: all
          # Online audits are the reason this job is worth having. Every offline
          # audit is reproducible from the pre-commit hook; only these can tell
          # that a pinned SHA is not in the repository it claims to come from,
          # or that a version comment no longer matches its pin.
          online-audits: true
          min-confidence: medium
          min-severity: low

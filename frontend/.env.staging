# Loaded by `vite build --mode staging`, on top of frontend/.env.
# Public values only — never put secrets in a VITE_* variable, they are
# inlined into the client bundle.
VITE_API_URL=https://api.staging.shop-n-cook.com
VITE_PROJECT_NAME="Shop'n'Cook (staging)"
